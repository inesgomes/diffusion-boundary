repos:
  - repo: local
    hooks:
      - id: export-environment
        name: Export Conda Environment
        entry: bash -c 'mamba env export --no-builds | grep -v "^prefix:" > environment.yml && git add environment.yml'
        language: system
        always_run: true
        pass_filenames: false
