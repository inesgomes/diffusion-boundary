project: 20241121_diffusion

device: cpu
seed: 42

dataset:
  name: cifar10

classifier:
  name: hf_hub:SamAdamDay/resnet18_cifar10 # hf_hub:edadaltocg/resnet50_cifar10
  lib: timm

diffusion:
  pipeline: noguidance
  type: pndm # pndm, ddpm, ddim
  name: google/ddpm-cifar10-32
  args:
    num-inference-steps: 100
    batch-size: 5
